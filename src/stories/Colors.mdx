import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

<Meta title='Colors' />

<ColorPalette style={{ wordBreak: 'break-word' }}>
  <ColorItem
    title='primaryColor'
    subtitle='Primary Color'
    colors={['var(--primaryColor)']}
  />
  <ColorItem
    title='secondaryColor'
    subtitle='Secondary Color'
    colors={['var(--secondaryColor)']}
  />
  <ColorItem
    title='adaptive'
    subtitle='Adaptvie'
    colors={[50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950].map(
      (step) => `var(--adaptive${step})`
    )}
  />
  <ColorItem
    title='adaptiveOpacity'
    subtitle='Adaptvie Opacity'
    colors={[50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950].map(
      (step) => `var(--adaptiveOpacity${step})`
    )}
  />
  <ColorItem
    title='whiteOpacity'
    subtitle='White Opacity'
    colors={[50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950].map(
      (step) => `var(--whiteOpacity${step})`
    )}
  />
  <ColorItem
    title='blackOpacity'
    subtitle='Black Opacity'
    colors={[50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950].map(
      (step) => `var(--blackOpacity${step})`
    )}
  />
  <ColorItem
    title='red'
    subtitle='Red'
    colors={[50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950].map(
      (step) => `var(--red${step})`
    )}
  />
  <ColorItem
    title='green'
    subtitle='Green'
    colors={[50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950].map(
      (step) => `var(--green${step})`
    )}
  />
  <ColorItem
    title='blue'
    subtitle='Blue'
    colors={[50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950].map(
      (step) => `var(--blue${step})`
    )}
  />
</ColorPalette>
